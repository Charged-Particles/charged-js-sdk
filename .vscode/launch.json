{
    "configurations": [
    


        {
            "type": "node-terminal",
            "name": "Run Script: Charged test",
            "request": "launch",
            "command": "yarn run test Charged.test.ts --no-cache",
            "cwd": "${workspaceFolder}"
        }
    ]
}