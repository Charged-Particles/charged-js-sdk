{
    "configurations": [
    


        {
            "type": "node-terminal",
            "name": "Run Script: Chaged test",
            "request": "launch",
            "command": "yarn run test Charged.test.ts --no-cache",
            "cwd": "${workspaceFolder}"
        }
    ]
}