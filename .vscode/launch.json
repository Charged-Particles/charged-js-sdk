{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: test",
            "request": "launch",
            "command": "yarn run test Charged.test.ts",
            "cwd": "${workspaceFolder}"
        }
    ]
}