{
    "configurations": [

        {
            "type": "node-terminal",
            "name": "Run Script: test",
            "request": "launch",
            "command": "yarn run test ChargedNetwork.test.ts --no-cache",
            "cwd": "${workspaceFolder}"
        }
    ]
}